# Project Rules

1. **Active Workspace**: The active workspace for this project is strictly `C:\Users\prash\Downloads\Sorting Algorithm Web App`. Do NOT perform edits or run commands in the old scratch directory.
2. **Auto-commit and Push**: Whenever you (the agent) complete a task, make updates, or finish a phase, you MUST automatically commit the changes to Git and push them to the connected GitHub repository using `git add .`, `git commit -m "..."`, and `git push origin main`.
